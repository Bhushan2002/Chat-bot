import 'package:flutter/material.dart';

class TranslationFeature extends StatelessWidget {
  const TranslationFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
